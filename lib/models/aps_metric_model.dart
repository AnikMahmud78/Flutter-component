class ApsMetricModel {
  final String employeeId;
  final double performanceIndex;
  final DateTime calculatedAt;

  const ApsMetricModel({
    required this.employeeId,
    required this.performanceIndex,
    required this.calculatedAt,
  });
}
